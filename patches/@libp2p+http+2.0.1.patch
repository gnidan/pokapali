diff --git a/node_modules/@libp2p/http/dist/src/middleware/origin.js b/node_modules/@libp2p/http/dist/src/middleware/origin.js
index edf8b36..bff48fb 100644
--- a/node_modules/@libp2p/http/dist/src/middleware/origin.js
+++ b/node_modules/@libp2p/http/dist/src/middleware/origin.js
@@ -1,14 +1,20 @@
-import { toURL } from '@libp2p/http-utils';
+import { toURL } from "@libp2p/http-utils";
 export class Origin {
     async prepareRequest(resource, opts) {
-        if (opts.headers.get('origin') != null) {
+        if (opts.headers.get("origin") != null) {
             return;
         }
-        if (opts.mode === 'no-cors') {
+        if (opts.mode === "no-cors") {
             return;
         }
         const url = toURL(resource, opts.headers);
-        opts.headers.set('origin', `${url.protocol}//${url.host}`);
+        const origin = `${url.protocol}//${url.host}`;
+        try {
+            opts.headers.set("origin", origin);
+        } catch {
+            const h = new Headers(opts.headers);
+            h.set("origin", origin);
+            opts.headers = h;
+        }
     }
 }
-//# sourceMappingURL=origin.js.map
\ No newline at end of file
diff --git a/node_modules/@libp2p/http/dist/src/middleware/peer-id-auth.js b/node_modules/@libp2p/http/dist/src/middleware/peer-id-auth.js
index e66e2b1..3bebab8 100644
--- a/node_modules/@libp2p/http/dist/src/middleware/peer-id-auth.js
+++ b/node_modules/@libp2p/http/dist/src/middleware/peer-id-auth.js
@@ -26,7 +26,7 @@ export class PeerIdAuth {
             if (opts.headers.get('authorization') != null) {
                 throw new InvalidParametersError('Will not overwrite existing Authorization header');
             }
-            opts.headers.set('Authorization', authorization);
+            try { opts.headers.set("Authorization", authorization); } catch { const h = new Headers(opts.headers); h.set("Authorization", authorization); opts.headers = h; }
         }
     }
     async getOrCreateAuthToken(resource, opts) {
