---
"@pokapali/core": patch
---

Add epoch domain types: Edit (with origin, signature), Epoch (trailing boundary, unordered edit bag), EpochBoundary (open | closed | snapshotted discriminated union).
