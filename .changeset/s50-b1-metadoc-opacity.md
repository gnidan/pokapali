---
"@pokapali/codec": minor
"@pokapali/core": patch
---

Add SurfaceMap interface and getMap()/transact() to
CodecSurface for codec-agnostic shared map access.
Remove last `as Y.Doc` cast in core production code.
