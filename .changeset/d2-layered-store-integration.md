---
---

Add integration + failure-injection tests for the A2
layered BlockResolver: interleaved success/failure
sequences, disjoint invariant under LRU eviction,
hydration + concurrent put races, and property-based
model testing against the real impl.
